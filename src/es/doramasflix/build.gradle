plugins {
    id 'symera.plugins.extension' apply false
}

ext {
    extName = 'Doramasflix'
    extClass = '.DoramasflixFactory'
    extVersionCode = 9
}

apply plugin: "symera.plugins.extension"

android {
    sourceSets {
        test {
            java.srcDirs = ['test']
            kotlin.srcDirs = ['test']
        }
    }
}

dependencies {
    implementation(project(':lib:fkplayer'))
    implementation(project(':lib-multisrc:pelisplus'))
    testImplementation(libs.bundles.common)
    testImplementation(libs.junit)
}
