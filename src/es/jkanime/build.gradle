plugins {
    id 'symera.plugins.extension' apply false
}

ext {
    extName = 'Jkanime'
    extClass = '.JkanimeFactory'
    extVersionCode = 26
}

apply plugin: "symera.plugins.extension"

dependencies {
    implementation(project(':lib:byse'))
}

dependencies {
    implementation(project(':lib:dood'))
    implementation(project(':lib:okru'))
    implementation(project(':lib:mixdrop'))
    implementation(project(':lib:mp4upload'))
    implementation(project(':lib:filemoon'))
    implementation(project(':lib:streamtape'))
    implementation(project(':lib:voe'))
    implementation(project(':lib:vidhide'))
    implementation(project(':lib:streamwish'))
    implementation(project(':lib:streamsb'))
    implementation(project(':lib:universal'))
}
